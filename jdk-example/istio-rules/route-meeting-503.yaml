apiVersion: config.istio.io/v1alpha2
kind: RouteRule
metadata:
  name: meeting-server-503
spec:
  destination:
    name: meeting-server
  precedence: 10
  httpFault:
    abort:
      percent: 100
      httpStatus: 503
